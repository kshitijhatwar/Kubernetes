kkind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
- role: control-plane
- role: control-plane
- role: worker
- role: worker
- role: worker
- role: worker
- role: worker

#save this file with name clusterconfig.yml
# Use this command             #kind create cluster --name my-cluster --config=clusterconfig.yml
# This will create 2 masternode cluster and 5 worker nodes.
